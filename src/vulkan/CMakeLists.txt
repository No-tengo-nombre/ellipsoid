# set(PROJECT_VULKAN_NAME "${PROJECT_NAME}Vulkan")

# file(GLOB VULKAN_ROOT_SOURCE *.cpp)
# file(GLOB VULKAN_ROOT_HEADER *.hpp)

# add_library(${PROJECT_VULKAN_NAME} SHARED
#     ${VULKAN_ROOT_SOURCE}
#     ${VULKAN_ROOT_HEADER}
# )
